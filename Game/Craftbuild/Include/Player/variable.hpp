#pragma once

namespace Craftbuild {
    const float PLAYER_WIDTH = 0.6f;
    const float PLAYER_HEIGHT = 1.8f;
    const double DOUBLE_TAP_THRESHOLD = 0.25;
    const float FIXED_DT = 1.0f / 60.0f;
}